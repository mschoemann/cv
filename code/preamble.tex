\usepackage{marvosym} % Allows the use of symbols
\usepackage{parskip} % Spacing adjustments


% Main font

\usepackage{fontspec}
\setmainfont{Raleway}[
  Path           = ../fonts/,  % Adjust the path to match where you've placed the fonts
  Extension      = .ttf,      % or .otf, depending on the font files you have
  UprightFont    = *-Regular,
  BoldFont       = *-Bold,
  ItalicFont     = *-Italic,
  BoldItalicFont = *-BoldItalic
]
\setsansfont{Raleway}[
  Path           = ../fonts/,  % Adjust the path to match where you've placed the fonts
  Extension      = .ttf,      % or .otf, depending on the font files you have
  UprightFont    = *-Regular,
  BoldFont       = *-Bold,
  ItalicFont     = *-Italic,
  BoldItalicFont = *-BoldItalic
]


% Header and footer

\usepackage{fancyhdr}
\usepackage{lastpage} % Required for \pageref{LastPage}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[L]{\textcolor{gray}{\footnotesize\emph{Updated \today}}}
\fancyfoot[R]{\textcolor{gray}{\footnotesize\emph{Available at \href{https://mschoemann.github.io/cv/cv_mschoemann.pdf}{mschoemann.github.io/cv}}}}
\fancyfoot[C]{\textcolor{gray}{\footnotesize\emph{Page \thepage\ of \pageref{LastPage}}}}
\renewcommand{\headrulewidth}{0pt} % Removes the header line
\renewcommand{\footrulewidth}{0pt} % Removes the footer line

% Quotes
% \usepackage{csquotes}

% Logos
\usepackage{svg}

% ORCID
\newcommand{\orcid}[1]{\href{https://orcid.org/#1}{\includesvg[width=10pt]{logos/orcid.svg} #1}}
\newcommand{\orcidlogo}[1]{\href{https://orcid.org/#1}{\includesvg[width=8pt]{logos/orcid.svg}}}

% Badges
\newcommand{\badgedata}{\includesvg[width=8pt]{logos/badge_data_small_color.svg}}
\newcommand{\badgematerial}{\includesvg[width=8pt]{logos/badge_materials_small_color.svg}}
\newcommand{\badgeprereg}{\includesvg[width=8pt]{logos/badge_prereg_small_color.svg}}
\newcommand{\logooa}{\includesvg[width=8pt]{logos/logo_oa_color.svg}}
\newcommand{\logopsyarxiv}{\includegraphics[width=8pt]{logos/logo_psyarxiv.png}}
